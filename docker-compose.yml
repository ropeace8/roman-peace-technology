services:
  blog-app:
    container_name: blog
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ${CONTENT_DIR}:/usr/share/nginx/content:ro
    restart: unless-stopped
    networks:
      - edge

networks:
  edge:
    external: true
    name: edge
